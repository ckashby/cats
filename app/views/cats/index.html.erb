<h1>Cats index</h1>
<p>List cats here...</p>

<% @cats.each do |cat| %>

<%= link_to cat.name, cat %> | <%= cat.gender %> | <%= link_to "Edit", edit_cat_path(cat) %> |  
<%= link_to 'Destroy', cat, method: :delete, data: { confirm: 'Are you sure?' } %>
<hr>

<% end %>

<%= link_to "Create new cat", new_cat_path, class: 'btn btn-warning m-3 px-6' %>
